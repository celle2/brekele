from setuptools import setup

setup(
    name = "brekele.py",
    version = "0.1",
    py_modules = ['brekele'],
    url = "https://github.com/celle2/brekele",
    author = "celle2",
    install_requires = ["pyngrok"]
)
